# Pass 1 — Inputs

**P1-01** | Inputs | H | `index.html:3020` | `importData`'s recipes branch assigns any parsed JSON straight to `recipesData` and persists it, with no shape check at all. | Line 3017 routes a file to the shopping branch only if its name contains `shopping` or it has both `shoppingList` and `neededList`; anything else falls to 3020 `recipesData = migrateIngredientsIfStale(parsed, window.__fmaSeedRecipes)` followed by `saveRecipesLocal()`. `migrateIngredientsIfStale` (749) never establishes `data.recipes` — it normalises `data.ingredients` to an array (769) and calls `ensureSettings`, but the only two `Array.isArray(...recipes)` tests in the file are `migrateCookedStamps:803` (which merely returns early) and `importSharedRecipes:3328`. So a `trip-history.json` or a renamed share bundle produces a `recipesData` with no `recipes` array that is written to localStorage; `init:843` then reads `recipesData.recipes.length` and throws on every subsequent load. | Confidence: High

**P1-02** | Inputs | M | `index.html:3017` | A file whose name merely contains "shopping" reaches `adoptImportedShopping` with no check that `shoppingList` or `neededList` exist, and `neededList` is read unguarded on every live path. | `adoptImportedShopping:2985` validates only `if(!parsed) return false` (2986); it then `Object.assign({}, parsed)`s and calls `applyMergedShopping` (3006), which persists to localStorage (2489) without validating shape. `neededList` is then dereferenced with no `|| []` at 3618, 3809, 3826, 3837, 5201, 5212, 5218, 5226, 5227 and 5252, while the pure merge helpers at 2173, 2258 and 3794 all guard it with `|| []` and the sibling `shoppingList` is guarded at its readers (2498, 3566). Opening the Wait List tab or generating a list after such an import throws. | Confidence: High

**P1-03** | Inputs | M | `index.html:3466` | `parseQty` divides by a user-supplied denominator with no zero check, so `"1/0"` yields `Infinity`, while the sibling parser for the same input class guards it. | Line 3466 is `if(/^\d+\/\d+$/.test(s)){ const [a,b] = s.split('/'); return parseFloat(a)/parseFloat(b); }` — no denominator test. `parseMeasureQty:5590` handles the same fraction syntax and does `const den=parseInt(m[3],10); if(!den) return null`. Downstream: `fmtQty:3470` returns the string `"Infinity"`; `stapleQtyToShopping:667` admits it because the only range test is `!(n > 0)`; `generateShoppingList:3592` accumulates `qn * factor` into `totalQty`. `JSON.stringify(Infinity)` is `null`, so the stored staple amount silently becomes `null` on the next save/reload rather than being rejected at entry. | Confidence: High

**P1-04** | Inputs | M | `index.html:4397` | Servings is read as `parseInt(...) || fallback`, which passes negative values, and the resulting scale factor goes negative. | Five entry sites — 4397, 4401, 4595, 4637, 4650 (and 6639 on the import workbench) — all use `parseInt(input.value,10) || <default>`; `parseInt("-3",10)` is `-3`, truthy, so the fallback never fires. The `min:'1'` on the inputs (4592, 4633, 6550) is an HTML hint and the inputs are not inside a validated `<form>`. `generateShoppingList:3580` computes `const factor = (sel.servings || recipe.servings || 1) / (recipe.servings || 1)` and 3592 does `lines[key].totalQty += qn * factor`, producing negative shopping quantities. The two Settings number inputs handling the same kind of value do range-check and write the clamp back: 5912 and 5932 both use `(v && v > 0) ? v : <default>`. | Confidence: High

**P1-05** | Inputs | M | `index.html:817` | The two essential localStorage blobs are parsed with no try/catch and no shape check, unlike every other storage read in the file. | 817 `recipesData = lsR ? JSON.parse(lsR) : seedRecipes;` and 820 `shoppingData = lsS ? JSON.parse(lsS) : defaultShoppingData();` are bare. By contrast `loadUiState:377` wraps its parse in try/catch and whitelists `s.tab` against `VALID_TABS`, and `loadTripHistoryLocal:2744` wraps its parse *and* re-checks `!Array.isArray(tripHistory.trips)` (2747) before accepting. A truncated or non-object value here throws out of `init`; the handler at 6653 prints "Startup failed" plus advice about opening the file from disk (6669), which does not describe this cause, and the offending value is left in localStorage so every reload fails identically. | Confidence: High

**P1-06** | Inputs | M | `index.html:526` | `syncClock.answered` assigns `latest` directly, bypassing the monotonic guard that the adjacent v24.0.1 comment states is the only way the clock takes an mtime. | `take` (520-523) guards both holders: `if(!held || tsOf(mtime) >= tsOf(held))` and the same for `latest`; the comment at 505-518 says "NEITHER of these goes backwards" and "Only this knows how to hold something. Both public holders go through it". But 526 is `if(status === 200){ contact(); if(mtime) latest = mtime; settle(); return; }` — an unconditional write. `settle` (496-499) then tests `latest !== held` as a raw string inequality, not via `tsOf`, so a `latest` that has moved backwards past `held` sets `behindSince` and the device reports itself behind a copy it already holds — the exact failure v24.0.1 fixed for `held`. Reachable by the same argument that comment makes: `fetchFolderListing:1655` calls `answered(200, …)` from `stampOneDriveTimestamps`, which its own comment (1631) says "runs at poll startup and after EVERY autosave", and `fetchRemoteShopping:2532` calls it from `writeShoppingMerged:2551` — neither is inside the `syncInFlight` guard that only covers the pollers (2605). The same string-vs-timestamp split means two spellings of one instant (`…:00Z` vs `…:00.000Z`) also read as "behind". | Confidence: Med

**P1-07** | Inputs | M | `index.html:6550` | Imported servings has all non-digits stripped before parsing, so a yield of "4-6" is read as 46. | `openImportWorkbench:6550` is `value:String(parseInt(String(data.servings||'').replace(/[^0-9]/g,''),10)||4)`; `"4-6".replace(/[^0-9]/g,'')` is `"46"`. The producer is the bookmarklet at 6468, which emits `servings:String(Array.isArray(rec.recipeYield)?rec.recipeYield[0]:(rec.recipeYield||''))` — schema.org `recipeYield` is routinely a range ("4-6", "4 to 6 servings"). The text-paste path in the same function family reads the same concept differently: `parseImportPaste:6489` matches `/serv(?:es|ings?)[:\s]*([0-9]+)|([0-9]+)\s*servings?/i` and captures only the first digit run, giving 4. Two readings of one input, and the 46 flows into the scale factor at 3580. | Confidence: High

**P1-08** | Inputs | M | `index.html:3364` | `importSharedRecipes` copies an ingredient master record wholesale out of an untrusted share file, so a kitchen measure can be installed as a `shoppingUnit`. | 3364 is `recipesData.ingredients.push(x.shared ? Object.assign({}, x.shared) : {…})`, taking `shoppingUnit` verbatim from `parsed.ingredients` (3334). `SHOPPING_UNIT_OPTIONS` (718) is documented at 711-717 as the whole vocabulary, and the comment states a cup/TBsp/tsp value "must NEVER become an ingredient's shopping unit" because it hard-locks the recipe editor's unit box. The only code that enforces that is the repair pass in `migrateIngredientsIfStale:762` (`if(kitchenMeasureCanon(mi.shoppingUnit)) mi.shoppingUnit = 'mL'`), which runs on the load paths (817, 1198, 1477) and not on import — so the bad unit is live until the next page load. The master-list editor takes a third position on the same input, keeping an unrecognised unit as an "(off-list)" option (6339-6341). | Confidence: Med

**P1-09** | Inputs | L | `index.html:3557` | Name-keyed bare objects are indexed by raw user-entered ingredient names, so a name colliding with `Object.prototype` is dropped or written onto the prototype. | `generateShoppingList` declares `const lines = {}` (3557) and `const prevByKey = {}` (3565) and keys them on `(ing.ingredientName||'').trim().toLowerCase()` (3583) with no `hasOwnProperty`; 3585 is `if(!lines[key]) lines[key] = {…}`, so for `key === '__proto__'` the truthy `Object.prototype` skips creation and 3592 `lines[key].totalQty += qn * factor` writes onto the prototype, while `constructor` silently produces no line. `mergeAuthored` has the same shape at `byId` (2101) and `seen[k]` (2110). The file is careful about exactly this elsewhere: `picksOnlyAdded:3554` uses `Object.prototype.hasOwnProperty.call(now, x.r)` and `buildIngredientUsageIndex:5747` uses a `Map`. Recipe ids are safe because `slugify:4429` strips `[^a-z0-9]`; ingredient names are never slugified. | Confidence: Med

**P1-10** | Inputs | L | `index.html:673` | `stapleQtyToShopping` lowercases the typed unit suffix for ml/L/g/kg but matches cup/TBsp/tsp case-sensitively against the raw text, so "1 Cup" and "1 tbsp" are silently discarded. | 668 is `const suffix = (m[2] || '').toLowerCase();`, used at 670-671 (`'ml'`, `'l'`) and 676-677 (`'g'`, `'kg'`). Line 673 instead does `if(MEASURE_ML[m[2]]) return n * MEASURE_ML[m[2]];` on the un-lowercased capture, and `MEASURE_ML:5582` is keyed `{ cup, TBsp, tsp }`. Any other casing falls through to `return null` (679), and `setStapleQty:710` only stores when `n !== null` — so the amount is dropped with no message, having already deleted the previous value at 708. | Confidence: High

**P1-11** | Inputs | L | `index.html:2005` | `graceMs` carries two incompatible conventions for the value 0 within one file, and the injected test seams beside it cannot express 0 either. | `otherSideDropped:2005` uses `graceMs === undefined ? MERGE_GRACE_MS : graceMs`, so 0 means "no grace"; `syncAlertState:958` uses `s.graceMs || SYNC_UNLINKED_GRACE_MS` and `listFreshness:1004` uses `s.graceMs || FRESHNESS_GRACE_MS`, where 0 means "use the default". The same `||` pattern makes 0 unreachable for `s.now` (1003), `s.unreachableMs` (1005), `s.minFailures` (1006) and `s.staleMs` (974). Also undocumented: a negative `graceMs` at 2005 widens rather than narrows the deletion window, since the expression is `added < (seen - graceMs)`. No in-app caller sets `graceMs` — the only references are `o.graceMs` passed through at 2124/2125. | Confidence: High

**P1-12** | Inputs | L | `index.html:1007` | The two pure sync predicates disagree on what an absent `signedIn` means. | `listFreshness:1007` is `const shared = !!s.backend && s.signedIn !== false;` — `undefined` counts as signed in. `syncAlertState:968` is `if(settled && s.backend === 'onedrive' && !s.signedIn)` — `undefined` counts as signed out and produces the "sign-in has expired" card. Both read the same concept from snapshots assembled at 1093 (`signedIn: !!msalAccount`) and 4744 (`signedIn: syncBackend() === 'onedrive' ? !!msalAccount : true`), which are themselves two different conventions for the folder backend. Production values are always real booleans, so the divergence is only reachable through the injection seam these functions exist to provide. | Confidence: High

**P1-13** | Inputs | L | `index.html:1388` | The primary folder-resolution branch accepts any 200 from the `FridgeList` path without checking that the item is a folder; the fallback branch does check. | `resolveFridgeListFolder:1385` requests `/me/drive/root:/FridgeList` and at 1388-1389 returns `{driveId, itemId}` from `j.remoteItem.parentReference` or `j.parentReference` with no `j.folder` test, so a *file* named `FridgeList` in the drive root is adopted as the sync folder. The `sharedWithMe` fallback at 1396 filters on `it.name === 'FridgeList' && it.folder && it.remoteItem`. Line 1388 also dereferences `j.remoteItem.parentReference.driveId` unconditionally, which throws on a shortcut item lacking `parentReference`. | Confidence: Med

**P1-14** | Inputs | L | `index.html:2758` | `recentTrips` treats 0 and any negative argument as "every trip", which is not what its contract says, and both call sites pass 0. | 2758 is `return (n && n > 0) ? trips.slice(Math.max(0, trips.length - n)) : trips.slice();`; the doc comment at 2755-2756 says only "The newest `n` trips, oldest first. Every memory feature reads through this." The sentinel is undocumented and the two callers, `5425` (`cookRateSummary(recentTrips(0), …)`) and `5947` (`atHomeStreaks(recentTrips(0), 3)`), both depend on it, so the stated contract and the actual one differ for the only values in use. | Confidence: High

**P1-15** | Inputs | L | `index.html:6473` | `parseImportPaste` trusts any clipboard JSON carrying a truthy `fridgeListImport` and does not check that `ingredients`/`method` are arrays. | 6473-6475: `if(j && j.fridgeListImport){ return {name:j.name||'', servings:String(j.servings||''), url:j.url||'', ingredients:j.ingredients||[], method:j.method||[]}; }`. A string value for either passes the `||` guard, and `openImportWorkbench:6566` then calls `(data.method||[]).join('\n')` — strings have no `.join` — while the ingredient loop iterates `data.ingredients`. The file's other file-borne import does validate this exact field: `importSharedRecipes:3334` is `const sharedIngs = Array.isArray(parsed.ingredients) ? parsed.ingredients : [];`, with `Array.isArray(parsed.recipes)` checked at 3328. | Confidence: Med

# Pass 2 — Method

**P2-01** | Method | M | `index.html:1860` | `chooseTripWinner`'s rule 1 tests direct supersession only, so a trip replaced twice loses to the very list the user replaced. | The test is `aWp.supersedes === bTrip` / `bWp.supersedes === aTrip` — an equality on one field, not reachability through a chain. `generateShoppingList` writes `supersedes: tripIdOf(shoppingData)`, i.e. only the immediately preceding trip, and no trip retains its ancestors. So for A2 (supersedes A1) vs T0 (ticked, held by an offline phone), rule 1 does not fire, rule 2 (`tripIsWorkedOn`, 1868) hands it to T0, and every poll re-decides the same way. The v21.9 freeze does not close this: it keys off `tripIsLive` — ticks on *this* device — so a phone planning at home can mint A1 then A2 freely. The approach assumes the supersession relation is always one hop deep; nothing in the minting path enforces that, and CLAUDE.md states the guarantee unconditionally ("A deliberate clear outranks a live trip, and that is rule 1 doing its job"). | Confidence: High

**P2-02** | Method | M | `index.html:1868` | Rules 2 and 3 of the winner cascade compare another device's authored stamps against this device's wall clock, with no skew allowance, inside a merge that budgets 90 s for skew elsewhere. | `tripIsWorkedOn(sd, now)` (1829) and `tripIsLive` (1821) compute `(now || Date.now()) - d.newestAt <= TRIP_LIVE_WINDOW_MS`, where `newestAt` comes from `tripDecisions` (1795) reading `checkedAt`/`removedAt`/`atHomeAt` — stamps written by whichever device made the decision, `sd` being the remote copy at the 1868 call site. Rule 3 then compares `tsOf(aWp.basedOn)` against `tsOf(bWp.basedOn)`, two values each copied out of a third device's `lastUpdated` via `syncClock.horizon()`. A phone 7 h slow has its genuinely fresh work read as expired and its list discarded; one 7 h fast is permanently "worked on". Every other cross-device comparison in the same file is explicitly skew-tolerant — `TICK_TIE_WINDOW_MS` (356), `MERGE_GRACE_MS` (1983, "modest clock skew") — or uses server mtimes rather than authored stamps (`syncClock`, 488). The assumption that the two devices' clocks agree to well inside six hours is unstated. | Confidence: High

**P2-03** | Method | M | `index.html:1853` | The winner cascade mixes a lineage relation, a content predicate and a timestamp, which cannot yield a total order, yet the merge's convergence depends on one. | `chooseTripWinner` is applied pairwise by `mergeShoppingData:2203`, and the result decides `shoppingList` wholesale. With three distinct trips the "beats" relation need not be transitive: rule 1 can give A≻B (A supersedes B), rule 2 can give B≻C (B has decisions, C none), and rule 3 can give C≻A (C's `basedOn` is newer, neither supersedes the other). Each device then merges the pair it happens to hold and writes a different winner, and the next poll reverses it. Three concurrent trips are reachable and acknowledged: `putBackReplacedList:3920` and `keepThisList:3969` each mint a further trip, and CLAUDE.md records v24.0 fixing a case that "made a third trip id out of a two-way argument". Rule 2's dependence on `now` also makes the order time-varying, so the same pair can be decided differently on successive polls as the six-hour window lapses. | Confidence: Med

**P2-04** | Method | M | `index.html:1933` | `mergeShoppingLine` resolves a line's *derived* content by `changedAt`, a stamp only flag taps move, so a phone that merely ticked something can overwrite quantities another phone rebuilt. | Line 1933 is `const out = Object.assign({}, xNewer ? y : x, xNewer ? x : y);` with `xNewer = tsOf(x.changedAt) >= tsOf(y.changedAt)` (1932), and the comment says composition "comes from the more recently touched copy". `changedAt` is written by `stampLine` and recomputed at 1945 from the three flag stamps — it tracks shopper taps, not derivations. But `totalQty`, `sources`, `unit` and `neededIds` change on a *same-trip rebuild* (`generateShoppingList` keeps `tripId` when `sameTripRebuild()` is true, 3568), so two phones on one trip legitimately hold different content for the same line. Phone A adds a recipe (Onion total 5); phone B, not yet rebuilt (total 3), ticks anything on that line; B's `changedAt` is newer, so B's 3 and B's shorter `sources` win. The `neededIds` case re-creates the v23.7 defect by a different route: losing the id means `syncNeededFromLine:3805` has nothing to look the Wait List entry up by. CLAUDE.md states lines are exempt from `mergeAuthored` because "the authored part of a line is its flags" — but the non-authored part is neither re-derived nor merged; it is picked by the authored part's clock. | Confidence: Med

**P2-05** | Method | M | `index.html:3593` | Quantities from every source are added into one running total labelled with the master ingredient's unit, with nothing requiring the contributing amounts to be in that unit. | `generateShoppingList` fixes the line's unit once at creation — `unit: (meta && meta.shoppingUnit) || ing.unit || ''` (3587) — then accumulates `lines[key].totalQty += qn * factor` (3593) from `parseQty(ing.quantity)` for every recipe line, never consulting that line's own `ing.unit`; the staple amount is added to the same total at 3655. The approach is only valid if every contributor is already expressed in the master unit. Three documented paths break that: `collectIngredientRows:5736` stores a free-text `unit` verbatim whenever the measure select is hidden, `addNewMasterIngredients:3413` installs that free text as the ingredient's `shoppingUnit` (only cup/TBsp/tsp are canonicalised, by `kitchenMeasureCanon:3383`), and `migrateIngredientsIfStale:779` later *fills in* `shoppingUnit` from the seed for any ingredient that had none — retroactively declaring a unit for lines authored when there was none. "1 kg" and "200 g" then sum to 201, and `rollUpQty:4693` only knows `g` and `ml`, so a `kg`-unit line never rolls. Nothing detects or reports the incoherence. | Confidence: High

**P2-06** | Method | M | `index.html:3431` | Every cross-collection relationship is keyed on the lowercased ingredient display name, and the rename operation updates one of the six places that reference it. | `renameMasterIngredient(data, ing, newName)` rewrites `data.ingredients` and `data.recipes[].ingredients[].ingredientName` (3439-3443) and nothing else. The same name is the key for `settings.staples` (an array of names, 632), `settings.stapleQty` (keyed by name, 703-710), `settings.alwaysAtHome` (names, 3692-3696), `lineMergeKey` (`'ing:' + name`, 1708) and the archive's `lines[].name` (2699). Consequences after one rename: the staple no longer resolves (`findIngredientMeta:3422` is exact-match) so `generateShoppingList:3645` emits a *second*, Uncategorised line under the old name alongside the recipes' line under the new one; the staple amount and the learned at-home entry are orphaned; and the live `shoppingList`'s key for that line no longer matches what a rebuild produces, so `prevByKey` (3566) misses and its tick is dropped on the next same-trip rebuild. The `data` parameter means the function structurally cannot reach `shoppingData`, which is in a separate file written by a separate `persist` branch (879-880). | Confidence: High

**P2-07** | Method | M | `index.html:2226` | A finish clears the list on every device but only the finishing device archives, so the append-only archive records one device's view of the trip while every memory feature reads it as the trip. | `mergeShoppingData` takes the later `shoppingDoneAt` from either side and sets `out.shoppingList = []` (2226) — after computing the union at 2216, which is then discarded in the same function. `archiveFinishedTrip:2765` is called only from `finishShopping:3838`, i.e. on the device that tapped it, and writes `tripRecordFromShopping(shoppingData, …)` from that device's pre-merge copy. So a line that existed only on a non-finishing phone (a manual extra added offline, or any line whose flags that phone held and had not yet pushed) is destroyed by the merge and never appears in any archive. `mergeTripHistory:2721` cannot repair it: its "fuller record wins" tiebreaker needs two records for the tripId and only one is ever written — the comment at 2717 says as much ("written once by the device that finished it"), which also means the tiebreaker it justifies can never fire. `cookRateSummary:4443`, `atHomeStreaks:4473` and the spend/never-bought work listed as next all read that partial record as the shop. | Confidence: Med

**P2-08** | Method | M | `sw.js:72` | The network-first timeout aborts on response *headers*, so the 1.3 MB body it exists to bound is fetched with no timeout at all. | `networkFirst` arms `controller.abort()` at `NETWORK_TIMEOUT_MS` (31), then `const fresh = await fetch(request, { signal: controller.signal, … })` (72) and `clearTimeout(timer)` (73). `fetch` resolves when headers arrive; the body is consumed afterwards by the browser with the abort already cancelled. `NETWORK_TIMEOUT_MS`'s own comment (29-30) states the intent — "short enough that a dead connection doesn't leave someone staring at a white screen in aisle 7" — and the cached-page fallback at 80/83 is the mechanism. On the stated target condition (one bar of signal, a single-file app of ~1.3 MB per CLAUDE.md) headers return promptly and the body takes far longer than 4 s, so the fallback never runs and the user waits on the network regardless. A header-only timeout is the wrong instrument for a payload this size; the approach would need the body streamed under the same deadline. Related: `cache.put` at 77 is neither awaited nor wrapped in `event.waitUntil`, so the entry the fallback depends on may never be written if the worker is killed after the response is returned. | Confidence: High

**P2-09** | Method | M | `index.html:4481` | `atHomeStreaks` treats an unstamped at-home as contrary evidence rather than as no evidence, so the pantry rule's own output breaks the streaks the feature is meant to find. | The loop is `if(l.atHome && l.atHomeAt) runs[l.name]++; else broken[l.name] = true;` (4481-4482). The comment (4472-4475) justifies only the first half — "Only DELIBERATE at-home taps count — atHomeAt is present only when somebody pressed the button" — which argues for *ignoring* an unstamped line, not for ending its run. A line that `pantryAtHome` set (`generateShoppingList:3680`, `l.atHome = true` with no stamp, archived with `atHomeAt: null` at 2709) therefore scores the same as an item genuinely needed and bought. With that feature on, every Pantry line the shopper did not explicitly tap breaks its own streak on every trip, so the `minRun`-of-3 test (4474, called with 3 at 5947) can essentially never be satisfied for the Pantry category — the one the suggestion exists to learn. | Confidence: High

**P2-10** | Method | M | `index.html:2087` | The tombstone pruning horizon is taken from the remote file's self-reported `lastUpdated`, so a single merge with a fast-clocked device discards this device's entire removal history. | `pruneTombstones(map, now)` computes `const at = tsOf(now) || Date.now();` (2087) and keeps only ids where `(at - tsOf(map[id])) < TOMBSTONE_MAX_AGE_MS` (2090). `now` is supplied by `mergeShoppingData:2237` as `[a.lastUpdated, b.lastUpdated].reduce(max)` — `b` being the remote copy, and `lastUpdated` being written by `saveShoppingLocal:861` from that device's `new Date()`. A phone whose clock is a year ahead therefore sets `at` a year ahead for this merge, every local tombstone is "older than 60 days" relative to it, and the map is emptied and written back. The removal records are the only positive evidence the merge has (`mergeAuthored:2116`), so the affected deletions fall back to `otherSideDropped:2001`, which errs toward keeping — every deleted pick and Wait List entry that survives on any other copy resurrects. The surrounding design is explicit that device clocks are not trusted for this class of decision (the whole of `syncClock`, and `seenRemoteAt` existing because "`shoppingData.lastUpdated` cannot be used for this"), yet the prune horizon is exactly such a clock. | Confidence: Med

**P2-11** | Method | M | `index.html:3056` | `buildShareBundle` deep-copies whole recipe records, so the cooking history the share rules say it never carries travels inside them; the import side strips half of it. | 3055-3056 are `recipes: JSON.parse(JSON.stringify(chosen))` and `ingredients: JSON.parse(JSON.stringify(ings))` — a whole-object copy, not a projection of the fields a recipe is. `lastCooked` and `lastPlanned` live on the recipe object (written at 3577 and by the Menu tab's tick), so both ship. CLAUDE.md states "`buildShareBundle` takes recipe ids and emits only those recipes plus the ingredient-master entries they reference — never the shopping list, the Wait List, settings, staples or cooking history. A share is a copy that stays unconnected to the sender's data." `importSharedRecipes:3332` sets `copy.lastCooked = null;   // their cooking history is not yours` and says nothing about `lastPlanned`, which `recipeHistoryLabel:4074` renders as "Planned N days ago" and `recipeHistoryTime:4084` sorts on — so the receiving household sees the sender's planning history on a recipe it has never planned. The comment at 3332 names the intent that the export method defeats. | Confidence: High

**P2-12** | Method | M | `index.html:1687` | Both poll loops advance their change-detection marker before awaiting the action it gates, so a load that fails is never retried and the remote change is skipped until something else bumps the mtime. | `pollOneDriveForChanges` sets `oneDriveLastModified[item.name] = item.lastModifiedDateTime` inside the same loop that computes `changed` (1687-1688), then `await loadFromOneDriveOrSeed()` at 1691. If that load throws or its download fails, the markers already hold the new mtimes, so the next poll computes `changed === false` and the change is lost. `pollShoppingNow` has the same shape at 2623: `shoppingFastPollStamp = meta.lastModifiedDateTime;` precedes `await fetchRemoteShopping(token)` (2624), and the comment at 2617-2620 acknowledges the stamp can end up "ahead of anything merged" — it draws the right conclusion for the sync clock (use `answered`, not `merged`) but leaves the poll with no way back to that mtime. `fetchRemoteShopping:2534` swallows a failed body download (`catch(e){ syncClock.unreachable(); … }`) and returns `{ data: null }`, which is exactly the path that produces this. | Confidence: High

**P2-13** | Method | M | `index.html:3501` | `selectionsSignature` is stored in `shoppingData` but computed partly from `recipesData.settings`, two files synced by different rules and written independently. | The fingerprint includes `featureOn('staples')` and `(recipesData.settings.staples || [])` with `stapleQtyFor(n)` (3514-3517); it is saved as `shoppingData.weekPlan.lastGeneratedSignature` (3744) and compared by `shoppingListIsStale:3985`. `persist(which)` writes the two files on separate branches (879-880) and recipes can be *refused* outright on an ETag conflict (1533 onward), while shopping merges per item — so a device can hold a shopping file whose stored signature embeds a staples configuration its local recipes no longer have, or vice versa. `shoppingListIsStale()` then reports stale (or not) for reasons unconnected to the shopping data, and that feeds the one auto-rebuild the Review tab still performs (the v23.7 exception, whose first condition is `shoppingListIsStale()`). Two devices transiently disagreeing about `features.staples` compute different signatures for identical state and can each "correct" the shared list by regenerating it with and without staple lines. | Confidence: Med

**P2-14** | Method | L | `index.html:2334` | Ticks are described by a clamped net delta while every other change is a set difference, so a merge that takes ticks away on the same trip reports nothing. | `describeMerge` computes picks and Wait List changes as `count(from, to)` — ids present in one set and not the other (2315, 2321-2322) — but ticks as `ticksArrived: Math.max(0, ticks(after) - ticks(before))` (2334). Three ticks arriving while three others are unticked nets to 0 and is reported as no tick change at all; any same-trip merge that reduces the tick count is clamped to 0 and, because `d.changed` (2344) lists only the arrival counters and `tripReplaced`, `mergeReport:2349` returns null and nothing is said — while `applyMergedShopping` has already swapped the data. `ticksLost` (2343) covers only the different-trip case. The design goal stated at 2347 and in CLAUDE.md is "A merge that changes anything says so"; a non-negative net delta cannot express the change most worth saying. | Confidence: High

**P2-15** | Method | L | `index.html:3919` | The undo restores a whole-object `weekPlan` snapshot into a structure the merge resolves per item, discarding every removal record made since the stash. | `putBackReplacedList` does `shoppingData.weekPlan = Object.assign({}, replacement.weekPlan, { … })` (3919-3925), where `replacement.weekPlan` is a deep copy taken at stash time (2450). That object carries `selections` *and* `selectionsRemoved`, so the tombstones accumulated since — written by `noteRemoved` at 4607 and 4641 — are replaced by the older map. `mergeAuthored` treats a removal record as the only positive evidence of deletion (2116), so a recipe deselected after the fork is resurrected by the union and stays until somebody removes it again. This also couples picks to the list, which `mergeShoppingData:2231-2235` and CLAUDE.md deliberately separate ("Which generation of the LIST won says nothing about whose recipes are right"); the confirmation text at 3917 announces the pick restore but not the lost removals. | Confidence: Med

**P2-16** | Method | L | `index.html:1912` | The tick-tie rule is a stamp-proximity test standing in for a clock-skew allowance, and it is set an order of magnitude tighter than the skew the same merge budgets for. | `mergeFlag` applies the bias only when `Math.abs(at - bt) <= TICK_TIE_WINDOW_MS && (aVal || bVal)` (1912) — i.e. when the two *stamps* are close, which is not the same question as whether the two clocks are close. `TICK_TIE_WINDOW_MS` is 10 s (356) and its comment says "two phones are rarely perfectly in sync"; `MERGE_GRACE_MS` in the same merge is 90 s and its comment says it "covers the 600 ms write debounce and modest clock skew" (1983). At any skew above 10 s the bias stops applying to genuinely simultaneous taps, and it applies spuriously to taps 20 s apart on two clocks 15 s out. Outside the window the rule falls back to `at >= bt` (1915), so whose tick survives is decided by whose clock is further ahead. The assumption that skew is well under 10 s is unstated and contradicted by the constant 1 600 lines away. | Confidence: High

**P2-17** | Method | L | `index.html:4050` | `daysSinceStamp` computes elapsed 24-hour periods and the labels then read them as calendar days; a future stamp is silently clamped to "today". | 4052 is `const ms = Date.now() - new Date(stamp).getTime();` and 4053 `return Math.max(0, Math.floor(ms / (1000*60*60*24)));` — duration, with no reference to local midnight or timezone. `recipeHistoryLabel:4066-4068` renders 0 as "Cooked today" and 1 as "Cooked yesterday", so something cooked at 20:00 and viewed at 09:00 the next morning reads "Cooked today", and something cooked 47 h ago (two calendar days) reads "Cooked yesterday". The `Math.max(0, …)` turns any stamp ahead of the local clock — a device with a fast clock, or an imported recipe carrying the sender's `lastPlanned` (see P2-11) — into "Cooked today"/"Planned today" rather than anything a reader could question. The threshold comparison at 4069 is consistent with the setting's wording ("days without being cooked"); only the day-name labels are the mismatch. | Confidence: High

**P2-18** | Method | L | `index.html:4083` | `recipeHistoryTime` is the one stamp reader that bypasses `tsOf`, so an unparseable stamp yields NaN and makes the recipe-list sort comparator inconsistent. | 4083-4085: `const c = recipe.lastCooked ? new Date(recipe.lastCooked).getTime() : 0; const p = …; return Math.max(c, p);`. `tsOf:1711` exists precisely to coerce an unparseable stamp to 0 and is used everywhere else stamps are compared; `Math.max` with a NaN operand is NaN, and a comparator returning NaN leaves `Array.prototype.sort` free to produce any order for the whole list, not just the offending row. `daysSinceStamp:4052` is the only other raw `new Date(stamp)` reader. This is reachable from untrusted input: `importSharedRecipes:3332` nulls `lastCooked` but leaves `lastPlanned` exactly as the share file supplied it, and `buildShareBundle:3055` copies it verbatim. | Confidence: Med

**P2-19** | Method | L | `index.html:4712` | One quantity column uses two different precision policies, switching discontinuously at the rollup threshold. | `lineQtyText` calls `fmtExactQty` (6 decimal places, exact by instruction — 4703) when `rollUpQty` fires, and `fmtQty` (rounds to 2 decimals — 4470) when it does not (4713-4717). `rollUpQty:4694` fires only at `n >= 1000`. So 999.456 g displays as "999.46 g" while 1000.456 g displays as "1.000456 kg": below the threshold the exactness the comment at 4699-4702 argues for is abandoned, above it the display gains four digits nobody asked for. The comment explains why `fmtQty` is wrong for the rolled branch ("it rounds to two decimals, which would turn 1001 g into 1 kg") without addressing why rounding is acceptable in the other. | Confidence: High

**P2-20** | Method | L | `index.html:2443` | The replaced-list undo is held in memory with a stated lifetime of "until the page closes", and the app closes its own page on a timer the undo's own condition does not guard. | `let replacement = null` (2443) with the comment "In-session only, like the bulk-delete undo: it covers the minutes in which anybody would notice" (2440-2441), and `clearReplacement` (2470) as the only clearer. Two automatic reloads discard it: the daily `AUTO_RELOAD_MS` sweep (6695-6701) and the refocus reload after `REFOCUS_MIN_HIDDEN_MS` of 5 minutes hidden (6704-6712). `safeToReload:6684` guards only `currentTab === 'review' && shoppingInProgress()` plus pending saves, and `shoppingInProgress()` is `tripIsLive` — ticks only (1821) — so the v23.3 case the stash was widened for, a list with pruning and no ticks, is not protected: a phone pocketed for five minutes reloads and loses both the undo and `argued`, the only record that two phones disagreed. | Confidence: High

# Pass 3 — Recalculation

Expected values were derived before the code was read or run, from the conversion basis
stated at `index.html:5579-5581`, the user-facing contract in `collectIngredientRows`'s
error text, the display rule stated at `index.html:4698-4702`, ECMAScript date parsing,
and plain arithmetic. Each was then executed against the real functions extracted from
`index.html` by brace-matching into a VM sandbox.

**P3-01** | Recalculation | M | `index.html:5607` | `measureToShoppingQty` rounds to whole millilitres, so small kitchen measures depart from the stated conversion basis by up to 60% and the same amount written two ways gives two different totals. | Standard: comment at 5579-5581, "cup = 250 mL, TBsp = 20 mL, tsp = 5 mL". Derived ½ tsp = 0.5 × 5 = **2.5 mL**; executed `measureToShoppingQty(0.5,'tsp')` = **3**. Derived two ½-tsp lines = 5 mL (= 1 tsp); executed **6**. Derived ⅛ tsp = 0.125 × 5 = **0.625 mL**; executed **1** (+60%). Typical case agrees: derived 2 cup = 500, executed 500. The basis comment describes an exact equivalence and `Math.round` at 5607 is unqualified by any comment; `collectIngredientRows:5728` stores the rounded number as the recipe line's `quantity`, so the loss is permanent, not presentational. | Confidence: High

**P3-02** | Recalculation | H | `index.html:5594` | The 0.02 fraction tolerance accepts quantities the stated contract forbids, then relabels them as a fraction they are not and stores a number that disagrees with the label shown. | Contract: `collectIngredientRows:5723` tells the user "Allowed: whole numbers and ⅛ ¼ ⅓ ½ ⅔ ¾". Derived for input "0.35" with unit cup: 0.35 is neither whole nor any listed fraction → **reject**. Executed: `parseMeasureQty('0.35')` = 0.35; `validMeasureQty(0.35,'cup')` = **true** (5596-5599: `Math.abs(rem - 1/3) = 0.0167 < 0.02`); `formatMeasureQty(0.35)` = **"⅓"** (5604, same tolerance); `measureToShoppingQty(0.35,'cup')` = **88**. Derived value of the label it displays: ⅓ cup = 250/3 = 83.33 → 83, confirmed by executing `measureToShoppingQty(1/3,'cup')` = **83**. So the recipe is saved with `displayQty:'⅓'` and `quantity:88` (5727-5729): the line reads "⅓ cup" for ever while the shopping list is built from 88 mL, 6% higher. The accepted band is rem ∈ [0.3133, 0.3533], i.e. 78-88 mL all displayed as ⅓ cup. | Confidence: High

**P3-03** | Recalculation | M | `index.html:661` | The documented 1 g = 1 mL equivalence is applied on the recipe path and not on the staple path, so a kitchen measure entered for a gram-shopped staple is silently discarded. | Standard: 5580-5581, "cup = 250 mL, TBsp = 20 mL, tsp = 5 mL, and 1 g = 1 mL for gram-shopped ingredients". Derived `stapleQtyToShopping('1 tsp','g')` = 1 tsp = 5 mL = **5 g**; executed **null**. Executed `stapleQtyToShopping('1 tsp','mL')` = **5**, so the measure table is consulted for one unit and not the other (673 sits inside the `unit === 'mL'` branch only; the `unit === 'g'` branch at 675-678 accepts g and kg alone). `setStapleQty:710` stores only when the result is non-null and has already deleted the previous entry at 708, so the amount is dropped with no message. Control cases agree: derived ("2 L",'mL') = 2000, executed 2000; derived ("0.5 kg",'g') = 500, executed 500; derived ("0 g",'g') = null, executed null. | Confidence: High

**P3-04** | Recalculation | M | `index.html:4709` | The "exact by instruction: no rounding" rule holds only above the rollup threshold; below it every value within 0.01 of an integer is printed as that integer. | Standard: comment at 4698-4702 — "Exact by instruction: no rounding, so 1250 g is 1.25 kg and 1001 g is 1.001 kg, not '1 kg'". Derived 999.456 g → **"999.456 g"**; executed `lineQtyText({hasNumeric:true,totalQty:999.456,unit:'g'})` = **"999.46 g"**. Derived 0.991 g → **"0.991 g"**; executed **"1 g"**. Derived 0.995 g → **"0.995 g"**; executed **"1 g"**. Above the threshold the rule is honoured: derived 1000.456 g → "1.000456 kg", executed the same; derived 2125 mL → "2.125 L", executed the same; derived 1000 g → "1 kg" and 999 g → "999 g", both executed as derived. The divergence is `fmtQty:3470`, whose first test `Math.abs(n - Math.round(n)) < 0.01` snaps a ±0.01 band around every integer before any rounding happens — wider than the 2-decimal rounding its own comment describes. | Confidence: High

**P3-05** | Recalculation | M | `index.html:4709` | A counted ingredient is rendered as a fractional count, so the list asks for 4.5 eggs. | Standard: a shopping list is a buying instruction, and `COUNT_UNITS` (4682) with `displayUnit` (4683) and `unitLabel` (730, returns "each") is the code's own record of which units are bought in whole pieces. Derived 3 eggs in a recipe serving 4, picked for 6: factor 6/4 = 1.5, 4.5 eggs → buy **5**. Executed the real chain (factor at 3580, `totalQty += qn * factor` at 3593, `lineQtyText` at 4709) = **"4.5"**. Derived 1 egg, serves 3, picked for 4: 1.333 → buy **2**; executed **"1.33"**. The exact case agrees: derived 3 eggs serves 4 picked for 4 = 3, executed "3". `displayUnit('qty')` returns `''` so the rendered line is "4.5 Eggs": the unit word is suppressed for counted items but the number is never brought to a whole piece. | Confidence: High

**P3-06** | Recalculation | M | `index.html:3453` | Two ordinary ways of writing a recipe amount are not summable, so those amounts are left out of the total and printed as literal text instead. | Derived `parseQty('1 1/2')` = **1.5** — a mixed fraction is the commonest way to write an amount in a recipe, and the file's other quantity parser reads it: executed `parseMeasureQty('1 1/2')` = **1.5**. Executed `parseQty('1 1/2')` = **null** (3465-3466 accept only a bare decimal or a bare `a/b`). Derived `parseQty('1,5')` = 1.5 (`parseMeasureQty` normalises the comma at 5591, executed = **1.5**); executed `parseQty('1,5')` = **null**. Downstream, executed the real accumulation for two recipes each needing "1 1/2 tsp": derived **"3 tsp"**, actual **"1 1/2, 1 1/2"** — both amounts go to `textQtyParts` (3594) and appear as text, so the shopper gets no total. The glyph spelling does work: derived `parseQty('1½')` = 1.5, executed 1.5. | Confidence: High

**P3-07** | Recalculation | M | `index.html:3593` | The summed line total is floating-point order-dependent, and the difference survives into the synced JSON that `applyMergedShopping` uses to decide whether anything changed. | Derived: adding the same three scaled amounts is order-independent, so two devices must reach one value. Executed the real expression `totalQty += qn * factor` for amounts [100, 200, 50] at factor 1/3: forward order = **116.66666666666666**, reverse order = **116.66666666666664**; `JSON.stringify` of the two differs. Both render identically — `fmtQty` gives "116.67" for each — so nothing on screen shows it. `totalQty` is written into each `shoppingList` line (3591) and travels in the synced file, and `applyMergedShopping:2479` decides "did anything change" by `JSON.stringify(merged) === JSON.stringify(shoppingData)`. Two devices can iterate `weekPlan.selections` in different orders: one that has just appended a pick locally (`selections.push`, 4637) has not yet been through `mergeAuthored`'s sort (2128), and that sort's tiebreak is `String(idOf(p)).localeCompare(...)`, which is locale-dependent. The result is the v21.0/v21.5 mechanism — object-graph swap plus repaint on a sync that changed nothing a person could see. | Confidence: Med

**P3-08** | Recalculation | M | `index.html:4062` | An unreadable history stamp yields NaN, which is rendered to the user verbatim and makes the recipe-list sort comparator inconsistent for the valid rows too. | Derived: a stamp that cannot be parsed must not be presented as a number, and `tsOf:1711` is the file's own convention for coercing one to 0. Executed `daysSinceStamp('last Tuesday')` = **NaN** (4052 uses raw `new Date(...).getTime()`); `recipeHistoryLabel({lastCooked:'last Tuesday'})` = **{"text":"Cooked NaN days ago","stale":false}** — the guard at 4064 is `cooked !== null`, which NaN passes. `recipeHistoryLabel({lastPlanned:'next week'})` = **{"text":"Planned NaN days ago"}**. Derived `recipeHistoryTime` on the same = **0**; executed **NaN**. Sorting four recipes by that comparator: derived ascending **[b,d,a,c]** (b unreadable → 0 first, then 2025, 2026-01, 2026-06); executed **[a,b,d,c]** — `d` (2025) placed after `a` (2026-01), so one bad stamp misorders the readable entries as well. Reachable: `importSharedRecipes:3332` nulls `lastCooked` and leaves `lastPlanned` exactly as the share file gave it, and `buildShareBundle:3055` copies it verbatim. | Confidence: High

**P3-09** | Recalculation | L | `index.html:4050` | `daysSinceStamp` counts elapsed 24-hour periods while the labels built on it say "today" and "yesterday", and a stamp ahead of the clock is clamped to "today". | Derived from the labels' own wording (4066-4068): something cooked at 20:00 and viewed at 09:00 next morning is **yesterday**; executed `daysSinceStamp` for 13 h ago = **0** → "Cooked today". Derived 47 h ago (two calendar days) = **2**; executed **1** → "Cooked yesterday". Derived a stamp 26 h in the future = negative, i.e. not a valid elapsed figure; executed **0** → "Cooked today", because `Math.max(0, …)` at 4053 clamps it. The computation is `(Date.now() - new Date(stamp).getTime()) / 86400000` floored, with no reference to local midnight or offset, so the day-name labels cannot be right except by coincidence of the hour. | Confidence: High

**P3-10** | Recalculation | L | `index.html:5427` | The per-shop cooked figure is rounded to a whole number, so the sentence reports "at least 0 cooked" while naming a non-zero cooked count in the same breath. | Executed `cookRateSummary` on 6 shops with 2 picks each and exactly 2 of those picks cooked: **{shops:6, picked:12, cooked:2}** (matching the derivation). `perCooked = 2/6 = 0.3333`; `Math.round(perCooked)` = **0** (5427, 5432). The rendered sentence is **"12 meals planned, at least 2 cooked — about 2 picked a shop and at least 0 cooked."** Derived: a per-shop average of 0.33 cannot honestly be stated as "at least 0" alongside a count of 2 — "at least" makes a floor claim, and 0 is vacuous. Executed the whole band: any cooked count below shops/2 renders as "at least 0 cooked a shop" (0, 1 and 2 of 6 all give 0; 3 of 6 gives 1). The comment at 4438-4441 states erring downwards is deliberate for `cooked` itself; the rounding of the derived average is separate and unremarked. | Confidence: High

**P3-11** | Recalculation | L | `index.html:1711` | The two ISO 8601 spellings of one nominal date resolve to different instants, with the date-only form reading as the later of the two. | Standard: ECMAScript parses a date-only form as UTC and a date-time form carrying no offset as local time. Derived: under a UTC+1 zone the two spellings of the same nominal midnight must differ by 3600000 ms. Executed with `TZ=Europe/London`: `tsOf('2026-07-01')` = **1782864000000**, `tsOf('2026-07-01T00:00:00')` = **1782860400000**, difference **3600000 ms** — the date-only form is the later, which was the opposite of the sign in the derivation. Every comparison in the merge is a `>` or `>=` on `tsOf` output (`resolveAuthoredItem:2033-2040`, `otherSideDropped:2005`, `noteRemoved:2067`), so the same calendar day written two ways wins or loses by an hour. The app's own writers always emit `new Date().toISOString()` (offset `Z`); the mixed forms arrive through `importData:3016` and `importSharedRecipes:3326`, and through any hand-edited copy in the shared folder. | Confidence: Med

# Pass 4 — Omissions

Enumerated against the stated purpose: a family shopping list used on phones in a
supermarket, multiple devices, data synced through OneDrive, offline-capable, no build
step. Cases found handled correctly are not logged: the localStorage write failures on
both main files (856-872), wake-lock re-acquisition after the screen blanks (5501), print
mode cleared on both `afterprint` and the `matchMedia` transition (5306-5312), service
worker registration failing on `file://` or in private mode (53), MSAL blocked by a
sandbox or CDN (1292-1296), absent-versus-failed on a file read (1434-1435, 2521-2525),
the ETag guard and 412 re-merge on recipes and shopping (1550, 2584), overlapping polls
(`syncInFlight`), the fast poll's tab/hidden/modal guards (2599-2602), repaint suppression
while typing or printing (`safeToRepaint`), division by zero on the progress bar (4805),
an absent or malformed trip history (2744-2747), and Graph's throttling of `sharedWithMe`
(demoted to a fallback, 1374-1378).

**P4-01** | Omissions | H | `index.html:1348` | Absent: nothing stops a background poll from navigating the page away to Microsoft sign-in, which the autosave path states it will never do. | `getGraphToken` falls back to `msalInstance.acquireTokenRedirect(...)` (1356) on any silent-token failure — a full-page navigation away from the app. It is called from every background path: the 600 ms autosave timer (1577, 1581, 1591), `stampOneDriveTimestamps` (1662), the 20 s folder poll (1680), the 5 s shopping poll (2608), `pushTripHistory` (2783) and `fetchTripHistory` (2806). `autoSaveToOneDrive`'s own comment at 1573-1574 states the opposite guarantee — "Background autosave never prompts — only a user-initiated sign-in/sync does" — and then calls `getGraphToken` at 1577 for precisely that case. There is no `interactive: false` flag, no caller-side suppression, and no distinction in the function between a user gesture and a timer. A refresh token that has expired while the Review tab is open therefore redirects the phone to a login page every 5 seconds, mid-shop, discarding the in-memory `replacement` undo (2443) and any debounced write not yet flushed. | Confidence: High

**P4-02** | Omissions | H | `index.html:1568` | Absent: no retry is ever scheduled for a failed OneDrive write, while the message shown for a throttle states that the app will keep retrying. | On failure `autoSaveToOneDrive` re-sets the pending flag — `if(!okR) pendingSaveRecipes = true;   // keep it owed so a later save retries` (1594, 1600) — and returns. The debounce timer is armed only inside `autoSaveToOneDrive` (1573), whose sole caller is `persist()` (883), whose callers are all user actions. Grep for the app's recurring timers returns four: the 60 s sync-banner re-render (1152), the 20 s folder poll (1625), the 5 s shopping poll (2637) and the 10-minute reload check (6697). None of them flushes a pending write. So a write that fails because the phone walked out of signal at the checkout is never re-attempted unless somebody taps something else; `writeErrorMessage`'s 429/503/509 case (908-911) tells the user "This usually clears by itself — the app will keep retrying", which nothing implements. `navigator.onLine` and the `online` event are not used anywhere in the file, so reconnection is not a trigger either. | Confidence: High

**P4-03** | Omissions | M | `index.html:4013` | Absent: `render()` has no error boundary and empties the container before building, so any throw in a tab leaves a blank screen with no message — including one reached from the 5 s poll. | 4019 is `app.innerHTML = '';` followed by the six tab builders at 4020-4025, none wrapped. The file's only error boundary is on `init()` (6654-6673), which prints "The Fridge List could not start" — the startup path, the one least likely to be hit in a supermarket. There are 24 `render()` call sites and the background repaint route reaches it too (`repaintWhenSafe` → `rerenderPreservingScroll:4031` → `render`), so a line or stamp arriving from another phone that any builder cannot render destroys the working content first and then fails, leaving `#app` empty on this and every subsequent render. Pass-3 style inputs reach these builders: `recipeHistoryLabel` is called from `renderRecipesTab`, and `lineQtyText` from the Review tab. | Confidence: High

**P4-04** | Omissions | M | `index.html:3840` | Handled incorrectly: the "archive before clearing" guarantee is stated in two comments and enforced nowhere — the failure is swallowed and the return value discarded. | `archiveFinishedTrip` returns a boolean (2766 `if(!rec) return false;` … 2770 `return true;`) and `finishShopping` calls it as a bare statement at 3840, then clears the list at 3841. The comments make the guarantee explicit: 2761-2763, "synchronous on purpose: finishShopping deletes the list, so a trip that did not reach localStorage is gone for good", and 3838-3839, "Order matters: the trip goes into the archive while the list it is made of still exists. The next line is where a week's worth of ticks used to vanish for good". But `saveTripHistoryLocal:2751-2752` catches a `localStorage.setItem` failure and only `console.warn`s, so `archiveFinishedTrip` returns `true` having stored nothing — and a quota failure is likely here, since the same origin already holds the ~1 MB recipes blob, the shopping file and up to 52 trips. The two other local writers treat the same failure as reportable (`saveRecipesLocal:858`, `saveShoppingLocal:871` both set `storageBlockedReason` and call `setStatus`). There is no path by which a failed archive prevents, delays or even mentions the clear. | Confidence: High

**P4-05** | Omissions | M | `index.html:2792` | Absent: the trip archive is the one synced file written with no concurrency guard, so two devices archiving in the same window silently drop a week. | `pushTripHistory` writes through `writeOneDriveFile` (2792), which sends a plain `PUT` with `Content-Type` only (1444-1448) — no `If-Match`, no ETag, no 412 handling. The other two synced files both have one: recipes via `recipesETag` with a refetch-and-retry (1519, 1550), shopping via `writeShoppingMerged`'s 412 re-merge loop (2584). `syncTripHistory:2773-2776` does `fetchTripHistory()` then `pushTripHistory()`, leaving the whole round trip as the race window. The design note at 2657-2660 argues the archive "cannot conflict" because "a trip is a CLOSED record, written once by the device that finished it" — which is about the merge function, not the write: a lost update discards a record the merge never gets to see. Two phones finishing their own shops within the same minute, or one archiving while another's `fetchTripHistory` is in flight, is exactly the multi-device case the app exists for. | Confidence: High

**P4-06** | Omissions | M | `index.html:1573` | Absent: no flush on page hide or close, so the 600 ms debounced write is lost whenever the phone is put away at the wrong moment. | Writes are debounced 600 ms in `autoSaveToOneDrive` (1573) and in `autoSaveToFolder` (1242 onward). Grep for `beforeunload`, `pagehide` and `freeze` over `index.html` and `sw.js` returns nothing; the three `visibilitychange` handlers that exist do other jobs — a poll (2646), the wake lock (5501) and the refocus reload (6706) — and none of them flushes `pendingSaveRecipes`/`pendingSaveShopping`. A phone screen locking, a tab discarded under memory pressure, or the PWA being swiped away inside that window loses the tick. The local copy is saved synchronously so nothing is lost on this device, but the other shopper never sees it, and `syncPendingSince` stays set so the phone will later accuse itself of a stall it could have avoided. `navigator.sendBeacon` and a `visibilitychange`-to-hidden flush are both absent. | Confidence: High

**P4-07** | Omissions | M | `index.html:856` | Absent: `navigator.storage.persist()` is never requested, so the local-first copy every feature reads is evictable. | Grep for `navigator.storage` over `index.html` returns zero matches. Everything durable on a device lives in localStorage under the keys at 324-327, 352, 361 and 2675: the recipes blob, the shopping file, the trip archive, the device id (`LS_DEVICE_ID`, which `tripId` and `supersedes` are built from) and the UI state. On iOS — the stated platform, "used on phones in a supermarket" — unpersisted site data is evicted after seven days without use, and all browsers evict under storage pressure. For a device that syncs, eviction silently resets `seenRemoteAt` (so `init:823`'s `syncClock.restore` restores nothing and the merge can prove it saw nothing — the v23.0 invariant's own failure mode) and changes the device id, minting a new identity in `chooseTripWinner`'s lineage. For a device with no backend — the case `syncAlertState`'s `unlinked` branch (961) exists to warn about — eviction is total data loss. Nothing requests persistence, checks `navigator.storage.estimate()`, or warns. | Confidence: High

**P4-08** | Omissions | M | `index.html:578` | Absent: no build or device identity travels in either synced file, and the field reserved for it is never written, in an app whose documented failure modes are all version skew. | `updatedBy` appears exactly once in the file — its declaration in `defaultShoppingData` (578) — so it is permanently `''`; grep confirms no writer. `APP_VERSION` (348) reaches only two UI strings (844, 5508) and the share bundle (3054); neither `shoppingData` nor `recipesData` carries it. CLAUDE.md states "`APP_VERSION` shows in Settings and is how two phones get compared", which makes the comparison a human reading two screens, and four of its "Known limitations" entries describe mixed-fleet behaviour that "self-corrects as devices update" — a phone on v23.4 or earlier crossing aisle ticks off everyone's Wait List, a phone on v23.1 or earlier writing no removal records, a phone on v21.9 polluting `lastCooked`, a shop finished on an older build not archived at all. Nothing in the app can detect any of them, name the device responsible, or tell anyone a phone needs updating. `describeMerge`/`mergeReport` (2322, 2349) report what a sync changed but never from where. | Confidence: High

**P4-09** | Omissions | M | `index.html:1711` | Absent: no server-time reference is ever taken, so the clock skew the merge's decisions rest on is undetectable. | Every authored stamp is local wall-clock: `new Date().toISOString()` at 861, 3558, 3812, 3838, 4499, 4545, 5201 and elsewhere. Several merge decisions then compare one device's stamps against another's, or against this device's `Date.now()`: `resolveAuthoredItem:2033-2040`, `otherSideDropped:2005`, `noteRemoved:2067`, `mergeFlag`'s `TICK_TIE_WINDOW_MS` band (1912), `tripIsLive`/`tripIsWorkedOn`'s six-hour window (1825, 1832), and `pruneTombstones`'s 60-day horizon taken from the remote file's own `lastUpdated` (2087, 2237). A server reference is available on every request and already partly used — `lastModifiedDateTime` feeds `syncClock` (526, 532, 1655) and every Graph response carries a `Date` header — but nothing ever compares it to the local clock, so a phone whose time is wrong by hours or days produces stamps the merge trusts, and no code path notices or reports it. The constants that exist (10 s, 90 s) are fixed allowances chosen for "modest clock skew" (1983) with no measurement behind them. | Confidence: High

**P4-10** | Omissions | L | `index.html:59` | Handled incorrectly: the reload latch is set before the safety check, so a service-worker update that lands mid-shop disarms the reload for the rest of the session. | The `controllerchange` handler reads `if(reloading) return;` (58) then `reloading = true;` (59) and only afterwards consults `window.__fridgeListSafeToReload()` (62-63), returning without reloading when it is false. Because the latch is already set, the handler can never act again in that page's life. `skipWaiting` is called eagerly (`promote`, 40-42) and `sw.js:47-48` claims clients on activate, so the page then runs the old build under a new worker with a different `CACHE` name indefinitely. The comment at 60-61 says "the update simply lands on next launch", which holds only because the page is eventually closed; the latch means it will not land when the shop ends, which is the moment `safeToReload` was written to wait for. | Confidence: High

**P4-11** | Omissions | L | `index.html:5283` | Absent: no watchdog on print mode, so if neither print event fires the app silently stops updating and stops reloading. | `printSection:5296` adds `printing-shopping`/`printing-menu` to `document.body` and relies entirely on `clearPrintMode` being called by `afterprint` (5308) or the `matchMedia('print')` transition (5310-5312). While the class is present, `isPrinting()` (5283) makes `safeToRepaint:2412` return false, which makes `repaintWhenSafe` defer every background repaint — the 5 s poll's included — and `safeToReload:6684` return false. There is no timeout, no retry and no visible indication of the state, so in any browser or situation where both signals are missed the shopper's list silently stops showing the other shopper's ticks for the rest of the session. The two print signals were added because one of them was already known not to fire everywhere (comment at 5305-5306). | Confidence: Med

**P4-12** | Omissions | L | `index.html:2721` | Absent: the archive's `version` field is written on every save and never read, and a future version is silently relabelled as 1. | `defaultTripHistory:2681` and both writers emit `{ version: 1, … }` (2727, 2735), and grep shows no reader anywhere — `loadTripHistoryLocal:2747` checks only `Array.isArray(tripHistory.trips)`. `mergeTripHistory:2727` and `pruneTripHistory:2735` both hard-code `version: 1` into their output, so an archive written by a future build with a different record shape is merged record-by-record as if it were v1 and then written back stamped 1, losing the only evidence that it was not. The file is read from the shared folder (`fetchTripHistory:2817`), so this is the mixed-fleet path, and the archive is the one file whose records are never rewritten — making an unnoticed shape change unrecoverable rather than self-correcting. | Confidence: High

# Pass 5 — Consistency

**P5-01** | Consistency | M | `sw.js:23` | The build version is duplicated across two files with nothing checking they agree, in a project whose release notes record the pair drifting four times in one session. | `index.html:348` is `const APP_VERSION = 'v24.0.1';` and `sw.js:23` is `const CACHE = 'fridge-list-v24.0.1';`. `package.json` carries no version (grep for `v24.0.1` returns only those two lines). `test/run-tests.js:115` pulls `extractConst('APP_VERSION')` into the sandbox, but only so other extracted functions can reference it — no assertion compares it against `sw.js`. CLAUDE.md's release step 1 states the hazard in its own words: "`sw.js` has said 'bump CACHE on every release' in a comment since v21.2 and four releases went past it in one session, because this list only ever named `APP_VERSION`. Two version strings, one step." The project's stated testing principle is that a drift should "fail loudly" and that anything a release "claims to make impossible" gets a case; this pair is held together by a checklist line only. | Confidence: High

**P5-02** | Consistency | M | `index.html:2209` | The merged shopping-line array keeps encounter order while every sibling collection in the same function is canonically sorted for determinism — and the comparison that decides whether to repaint is order-sensitive. | In the same-trip branch, `order` is built by first-encounter as `primary.shoppingList` then `secondary.shoppingList` are walked (2209, 2215-2217), and `out.shoppingList = order.map(...)` with no sort. The two authored collections beside it are explicitly canonicalised: `mergeAuthored:2127-2129` sorts "Stable, order-independent: oldest first, ties broken by id", and `pruneTombstones:2092` rebuilds its map through `ids.sort()` "so the merge is stable". `primary` is whichever file has the later `lastUpdated` (2185), which flips with every local save, so the output order of the same line set depends on who saved last — and each device then persists its own order. `applyMergedShopping:2479` decides "did anything change" by `JSON.stringify(merged) === JSON.stringify(shoppingData)`, so a pure reordering reads as a change and triggers the object-graph swap plus repaint the v21.0/v21.5 invariant exists to avoid. | Confidence: Med

**P5-03** | Consistency | M | `index.html:5037` | The aisle-to-category relation has three independent sources, and the one the shopping screen uses takes whichever line it saw last. | `CATEGORY_AISLES` (5576) is the declared map and is read only by `aislesForCategory` in the master-list editor (6313-6321). The Review tab re-derives the relation per render: `shownLines.forEach(line=>{ aisleCategory[line.aisle||'Other'] = line.shoppingCategory || 'Other'; })` (5037) — a plain overwrite, so if two ingredients share an aisle but disagree on category (the master-list editor sets the two independently at 6350-6351, and `aisleSelect` accepts off-list values) the walking position of the whole aisle group is decided by the last line in an alphabetically-ordered array. The print path derives it a third time, per line, at 5321-5322. `knownAisles()` (5560) is a fourth vocabulary, collected from `recipesData.ingredients`. Nothing reconciles them or notices a disagreement. | Confidence: High

**P5-04** | Consistency | M | `index.html:5035` | "No aisle" has one name everywhere it is written and a different name in both places it is displayed, and that second name is also the category sentinel. | Every writer uses `'Uncategorised'` — 3365, 3417, 3588, 3625, 3660, 5868, 6350, 6448 — and `isUnsetAisle(v){ return !v || v === 'Uncategorised'; }` (747) is the test. Both display groupers instead default a missing aisle to `'Other'`: `groups[line.aisle||'Other']` (5035, and `aisleCategory[line.aisle||'Other']` at 5037) and `const aisle = line.aisle || 'Other';` (5322). `'Other'` is simultaneously the *category* sentinel — `isUnsetCategory(v){ return !v || v === 'Other'; }` (746), written at 3365, 3417, 3589, 3626, 3661, 6351 — so one string is the unset marker for two different taxonomies and, at 5037, is used as both the key and the value of the same map. `catSortIndex` (3480) gives `'Other'` and `'Uncategorised'` the same index, so they sort together while rendering as two differently-named headings. An archived line carries `aisle: l.aisle || ''` (2700), which is a third spelling of the same state. | Confidence: High

**P5-05** | Consistency | M | `index.html:3627` | A line's `source` records which block of code created it rather than what the line is, so a Wait-List-only line is stored and archived as a recipe line. | The Wait List fold at 3621-3628 creates a line with `source:'recipe'` when no recipe line exists for that ingredient, then sets `fromFridgeList = true` at 3629. So a Wait List entry that no recipe needs produces a line whose `source` says recipe and whose `fromFridgeList` says Wait List. The staple block has the same shape: `source:'staple'` only when it creates the line (3664), `fromStaples = true` when it decorates an existing one (3653). `tripRecordFromShopping:2703` archives `source: l.source || 'recipe'` and drops `fromFridgeList`/`fromStaples` entirely, so the archive mis-attributes every folded Wait-List and staple line as a recipe line — and `atHomeStreaks`/`cookRateSummary` read that archive. Meanwhile the live code never trusts `source` for this question: the pantry rule tests `!l.fromFridgeList && !l.fromStaples && l.source !== 'needed'` (3680, 3695), using all three fields because no one of them is sufficient. `neededId` (singular, 3636) and `neededIds` (plural, 3627/3636) are a second pair encoding one relation, with the singular present on only one of the two line shapes and `syncNeededFromLine:3805` falling back between them. | Confidence: High

**P5-06** | Consistency | M | `index.html:5583` | The allowed-fraction table exists in three places with three different scopes, and the measure names in a fourth. | `MEASURE_FRACTIONS = { cup:[1/8,1/4,1/3,1/2,2/3,3/4], tsp:[1/8,1/4,1/2,3/4], TBsp:[1/2] }` (5583) is the rule `validMeasureQty` enforces. The message shown when it rejects something restates the same table as prose: `unitVal==='cup' ? 'whole numbers and ⅛ ¼ ⅓ ½ ⅔ ¾' : unitVal==='tsp' ? 'whole numbers and ⅛ ¼ ½ ¾' : 'whole numbers and ½'` (5723) — hand-maintained, matching today, with no link to the constant. `FRACTION_GLYPHS` (5584) is a third copy listing all six fractions with no per-unit scope, and `formatMeasureQty:5601` applies it to every unit — so it will render ⅓ or ⅔ for a TBsp value that `MEASURE_FRACTIONS.TBsp` does not permit. The measure-name set is hardcoded again as `[shopUnit,'cup','TBsp','tsp']` (5613) rather than read from `MEASURE_ML`, and once more as return values in `kitchenMeasureCanon` (3386-3388). Adding or changing one measure requires five coordinated edits. | Confidence: High

**P5-07** | Consistency | M | `index.html:672` | The kilo/litre factor is defined twice, in two directions, with no shared constant. | `UNIT_ROLLUP = { g: { to: 'kg', per: 1000 }, ml: { to: 'L', per: 1000 } }` (4691) is consumed by `rollUpQty:4695` as a *divisor* (`n / r.per`). `stapleQtyToShopping` independently hardcodes the same relation as a *multiplier*: `if(suffix === 'l')  return n * 1000;` (672) and `if(suffix === 'kg') return n * 1000;` (677). Neither references the other. The two also disagree about the unit vocabulary they accept for the same quantity: `UNIT_ROLLUP` keys are `g`/`ml` matched after `.toLowerCase()` (4694), while 672-677 compare a lowercased suffix for `ml`/`l`/`g`/`kg` but use the raw capture for the measure table at 673 — so the same string normalisation is applied inconsistently within one ten-line function. | Confidence: High

**P5-08** | Consistency | M | `CLAUDE.md:362` | Two invariants state a guarantee about an identifier that does not exist in the code. | `grep -c 'shoppingSeenRemoteAt' index.html` returns **0**; CLAUDE.md names it twice. Line 362: "`lastContactText` reads `shoppingSeenRemoteAt`, which advances only on a real remote read, so 'never' cannot be flattered into something softer." Line 432: "**`shoppingSeenRemoteAt`** advances only when a remote copy is actually read, which is why every merge site goes through `mergeRemoteShopping`." The value moved inside `makeSyncClock` in the v24.0 restructure — `lastContactText(seenRemoteAt, now)` (1077) takes it as a parameter and `renderSyncAlert:1097` supplies `syncClock.snapshot().seenRemoteAt`. CLAUDE.md's own preamble calls itself "the rules", and the v22.2 entry exists so the wording guarantee can be checked; a reader verifying either claim by name finds nothing. | Confidence: High

**P5-09** | Consistency | M | `CLAUDE.md:624` | The archive is documented as having four readers and a route feature; the code has two readers and no route feature. | CLAUDE.md:624 states "**The archive is written but barely read.** Four features come off it so far." Line 594 describes the honesty of those readers: "Everything reading the archive is written to be honest about thin data — the route needs three sightings of an aisle, the cook rate says 'at least'." Grep over `index.html` for readers of the archive returns exactly two call sites: `cookRateSummary(recentTrips(0), …)` at 5425 and `atHomeStreaks(recentTrips(0), 3)` at 5947 (the other `recentTrips`/`loadTripHistoryLocal` references at 2757, 2768 and 2816 are the loader, the archiver and the merge). Grep for `route`, `aisleRoute` or `ROUTE` over `index.html` returns nothing — the feature whose thin-data guarantee is cited does not exist. The same section's forward list ("The obvious next ones, in rough order of value per line") therefore describes a starting point two features further along than the code. | Confidence: High

**P5-10** | Consistency | L | `index.html:1809` | `tripHasProgress` does not read `tripProgress`, so the one name that looks like the predicate form of another reports a different quantity. | `tripProgress:1774` returns `{ ticks, newestAt }` counting ticks. `tripHasProgress:1809-1812` returns `tripDecisions(sd).decisions > 0` — it never calls `tripProgress`. `tripIsLive:1821` is the one that uses `tripProgress`; `tripIsWorkedOn:1831` uses `tripDecisions`. So of the five names around these two quantities, the matching prefix pairs (`tripProgress`/`tripHasProgress`) measure different things and the non-matching pair (`tripProgress`/`tripIsLive`) measure the same thing. The file is emphatic that the two *concepts* must stay apart — "Two functions on purpose; do not tidy them back into one" (1817) — and CLAUDE.md devotes an invariant to it, but the naming points a reader at the wrong one of each pair. | Confidence: High

**P5-11** | Consistency | L | `index.html:1772` | One constant, named after one of the two questions, tunes both functions the file insists must be kept distinct. | `TRIP_LIVE_WINDOW_MS = 6 * 60 * 60 * 1000` (1772) is used at 1826 by `tripIsLive` (ticks: "is somebody in a shop right now") and at 1835 by `tripIsWorkedOn` (decisions: "is there work here worth keeping"). The comment at 1829-1830 calls the second "The same six-hour test against decisions rather than ticks" — the sameness of the number is deliberate, but the two questions have different consequences (one freezes the Start tab, the other defends a list in `chooseTripWinner:1868`) and no way to be tuned apart. A reader changing the window to fix the freeze silently changes which list survives a fork. | Confidence: High

**P5-12** | Consistency | L | `index.html:3791` | `pendingWaitListLines` returns Wait List entries, not lines. | The body is `return ((sd && sd.neededList) || []).filter(…)` (3794-3801) — `neededList` entries, whose fields are `id`/`text`/`done`/`addedAt`. Its only caller reads them as such: `pendingWaitListLines(shoppingData).map(n=> n.text)` (4886). "Lines" is the word this file uses throughout for `shoppingList` members (`lineMergeKey`, `findShoppingLine`, `mergeShoppingLine`, `lineQtyText`, `pendingWaitListLines`'s own local variable `line` at 3799), which carry `ingredientName`, not `text`. The function's doc comment (3785-3790) is about which lines carry which entry's id, which is probably where the name came from. | Confidence: High

**P5-13** | Consistency | L | `index.html:416` | Three unrelated grace periods share a naming convention, and two of them are reachable through one injected parameter name. | `SYNC_UNLINKED_GRACE_MS = 12000` (416) is how long to let startup settle before calling a phone disconnected; `FRESHNESS_GRACE_MS = 45*1000` (998) is how far behind the shared copy a device may be before it is worth saying so; `MERGE_GRACE_MS = 90*1000` (1983) is the clock-skew allowance before an absence counts as a deletion. All three are read through a parameter called `graceMs`: `s.graceMs || SYNC_UNLINKED_GRACE_MS` in `syncAlertState` (958), `s.graceMs || FRESHNESS_GRACE_MS` in `listFreshness` (1004), and the fourth argument of `otherSideDropped` (2005). The two pure state functions are fed from one `syncClock.snapshot()` by design (CLAUDE.md: "`listFreshness` and `syncAlertState` both read one `snapshot()`"), so a single injected `graceMs` means 12 s in one and 45 s in the other. | Confidence: High

**P5-14** | Consistency | L | `index.html:1255` | The write debounce is a bare literal in both write paths and is the stated basis for a constant 700 lines away. | `}, 600);` appears at 1255 (`autoSaveToFolder`) and 1612 (`autoSaveToOneDrive`) with no named constant. `MERGE_GRACE_MS`'s comment at 1983 justifies its value by that number — "covers the 600ms write debounce and modest clock skew" — and the sync-clock comment at 511 refers to "the 600 ms autosave timer" when explaining why two merge paths are not mutually excluded. So one magic literal in two places is load-bearing for a third constant's correctness and for a documented concurrency argument, and changing either occurrence leaves both explanations silently wrong. | Confidence: High

**P5-15** | Consistency | L | `index.html:1997` | A field and the function that reads it are both named for a creation time, while for pre-v23.0 items they hold an upper bound — and the backfill writes the bound into the field named for the creation time. | `effectiveAddedAt(item, fileAt)` returns `tsOf(item && item.addedAt) || tsOf(fileAt)` (1998). Its own comment (1991-1996) says the fallback is the file's `lastUpdated` because "A file written at T proves everything in it existed by T at the latest, which is exactly the bound needed"; CLAUDE.md states the distinction outright — "that is an upper BOUND, not a creation time, and v23.0 then used it as one". `backfillAuthoredStamps:2168-2174` then assigns that bound to `s.addedAt` / `n.addedAt`, after which no reader can tell a real creation stamp from a bound: `mergeAuthored:2127` sorts on `tsOf(p.addedAt)` and `resolveAuthoredItem:2046-2048` takes "the earlier of the two: an item exists from when it was first made" — both treating the field as the thing its name says. The distinction the v23.0/v23.2 invariant turns on survives only in prose. | Confidence: High

**P5-16** | Consistency | L | `index.html:882` | `markSyncPending` and `syncPendingSince` are OneDrive-only despite generic names, so the "changes have not reached sync" warning can never fire on the folder backend. | `persist` calls it under a backend test: `if(localStorage.getItem(LS_ONEDRIVE_FLAG) === '1') markSyncPending();` (882), while the line above routes folder writes (881). `markSyncPending:1134` and `markSyncOk:1139` are named for sync in general, and `syncAlertState`'s pending branch reads `s.pendingSince` with no backend condition (975-979), so its sentence "Changes have not reached OneDrive for N minutes" is the only thing narrowing it. A folder-linked device — which `autoSaveToFolder` writes blind with no read-back, per CLAUDE.md's first known limitation — therefore has no stall warning at all, and nothing in the names or in `syncAlertState` says so. This is the same shape as the v22.2 bug the pending case was widened for: "persist() sets [it] only when the OneDrive flag is on, so a phone that had never signed in never marked anything pending". | Confidence: High

**P5-17** | Consistency | L | `index.html:412` | "Stale" carries four unrelated meanings across the file with no qualifier distinguishing them. | `SYNC_STALE_MS` (412) means local changes unsaved for too long. `migrateIngredientsIfStale` (749) means the saved data's `meta.version` is older than the bundled seed's. `shoppingListIsStale` (3976) means the generated list no longer matches the current picks, staples and Wait List. `recipeHistoryLabel`'s returned `stale` flag (4068, 4073, 4076) means the recipe has not been cooked within `notCookedRecentlyDays`. The four are read in the same neighbourhoods — `shoppingListIsStale` and the freshness strip both feed the Review tab (4864, 4893), and `listFreshness`'s `behind` case concerns yet another sense of out-of-date without using the word. | Confidence: High

# Pass 6 — Outputs

**P6-01** | Outputs | M | `index.html:2628` | The merge's own sentence is produced and then overwritten by a generic one on every path that actually delivers another phone's changes, so the specific wording is never seen. | `mergeRemoteShopping:2414` ends with `if(applied){ const line = mergeReport(what); if(line) setStatus(line, 'ok'); }` and returns `applied`. Its caller in the 5 s poll then does `if(mergeRemoteShopping(remote.data, remote.modified)){ setStatus('Shopping list updated from the other shopper — ' + new Date().toLocaleTimeString(), 'ok'); }` (2627-2629) — `setStatus:886-889` sets `textContent`, so the first string is replaced synchronously and never rendered. The 20 s poll does the same: `await loadFromOneDriveOrSeed(); setStatus('Updated from OneDrive — ' + …)` (1691-1692), and the write path's own merge at 2555 is followed by `setStatus('Saved to OneDrive — ' + …)` (1604). So "3 recipes added to the week, 1 Wait List item cleared" and, worse, "Another phone's shopping list replaced the one on this device — 12 items ticked off here are not on it. Use 'Put back the list that was replaced' if that was wrong." are both discarded. `mergeReport`'s comment (2347-2348) and CLAUDE.md ("A merge that changes anything says so … produce the line a phone shows when it catches up") describe an output that reaches the screen only on the startup path. | Confidence: High

**P6-02** | Outputs | M | `index.html:2490` | `applyMergedShopping` cannot persist and says nothing at all — no status line, no banner — while still swapping the state in and reporting success. | 2489-2490: `try{ localStorage.setItem(LS_SHOPPING, JSON.stringify(shoppingData)); } catch(e){ storageBlockedReason = 'Could not save locally (' + … + ').'; }` — it sets the variable and, unlike the two sibling writers, does **not** call `setStatus` (compare `saveRecipesLocal:858` and `saveShoppingLocal:871`, which both do). `storageBlockedReason` is read in exactly one place, the startup block at 6656, so nothing surfaces it afterwards. The function then continues to `shoppingData = merged` (2488 precedes it) and `return true` (2495), so every caller — `mergeRemoteShopping`, `adoptImportedShopping`, `putBackReplacedList` — treats the merge as landed. The device renders the merged state for the rest of the session and loses it entirely on the next reload, with no signal at any point. This is the one write that happens without a user action behind it, so there is no later `persist()` to re-announce the failure either. | Confidence: High

**P6-03** | Outputs | M | `index.html:2414` | The one status message that reports discarded work is emitted in the success style. | `setStatus(line, 'ok')` at 2414 is the only call that renders a `mergeReport` string, and `mergeReport:2352-2357` returns the trip-replaced sentence — "Another phone's shopping list replaced the one on this device — N item(s) ticked off here are not on it. Use 'Put back the list that was replaced' if that was wrong." — through that same call. `.status-bar.ok` is green (`#bfe3ae`, 124) and `.status-bar.warn` is amber (`#f0c98a`, 125), and `setStatus:889` applies whichever it is handed. The app clearly has a severity convention and applies it elsewhere: `renderListFreshness:4763` picks `alert-card` (red, 223-224) for the two sync cases and `notice-card` (amber, 219) for the rest, with the reasoning written out at 4757-4758. The sentence the comment at 2350-2351 calls out as the one that "must not read like" catching up is painted as the same kind of news as "3 recipes added". | Confidence: High

**P6-04** | Outputs | M | `index.html:4241` | An untrusted URL is rendered as a live link and copied into shared HTML with no scheme check. | `sourceUrl` has one writer: `sourceUrl: data.url || ''` at 6641, where `data.url` comes from `parseImportPaste:6474` as `url: j.url || ''` — any JSON on the clipboard carrying a truthy `fridgeListImport` (6473). The recipe viewer renders it as `el('a',{href:r.sourceUrl, target:'_blank', rel:'noopener'},['Source website ↗'])` (4241) with no validation; `target` and `rel` do not constrain a `javascript:` or `data:` scheme. The same value goes into the clipboard HTML at 3181 — `'<p>From: <a href="' + esc(r.sourceUrl) + '">' + …` — where `esc` (3141-3142) escapes `& < > "` and so cannot neutralise a scheme, and that HTML is also inserted into the live DOM by `copyHtmlBySelection:3222` via `el('div',{html: html})`, the file's only `html` attribute use. `buildShareBundle:3055` deep-copies the whole recipe, so the value propagates to other households, and `importSharedRecipes:3341-3345` copies it through without touching it. No scheme allowlist exists anywhere in the file. | Confidence: High

**P6-05** | Outputs | M | `index.html:2479` | The change detector compares serialised forms, so an in-memory value that does not survive JSON is invisible to it — the local copy is never corrected and two devices render the same line differently, permanently. | `applyMergedShopping:2479` is `if(JSON.stringify(merged) === JSON.stringify(shoppingData)) return false;`. Executed the real chain: `parseQty('1/0')` returns `Infinity` (pass-3 evidence; 3466 has no denominator guard), so a line can hold `totalQty: Infinity` with `hasNumeric: true`. The PUT body is `JSON.stringify(shoppingData)` (2560), and `JSON.stringify` maps `Infinity` to `null` — verified: the wire form is `{"ingredientName":"Flour","totalQty":null,"hasNumeric":true,"unit":"g",…}`. Executed both renderings: the writing device shows **"Infinity kg"** (`rollUpQty` divides Infinity by 1000 and `fmtExactQty` prints it), every other device shows **" g"** (`fmtQty(null)` is `''` while `hasNumeric` still gates the unit on). The two line objects stringify **identically**, so the equality test at 2479 returns false-for-changed and the corrected remote value is never adopted — the discrepancy is permanent and undetectable by the mechanism built to detect it. Nothing validates `totalQty` before the PUT, and nothing checks that `hasNumeric: true` implies a finite number. | Confidence: High

**P6-06** | Outputs | M | `index.html:858` | A local-save failure is announced only through a channel the next success overwrites, typically 600 ms later on the same user action. | `saveRecipesLocal:858` and `saveShoppingLocal:871` both report via `setStatus(storageBlockedReason, 'warn')`. `persist:878-883` calls the local save and then `autoSaveToOneDrive`, whose debounced timer fires 600 ms later (1612) and on success calls `setStatus('Saved to OneDrive — ' + …, 'ok')` (1604); the folder path does the same at 1254. So a tick that failed to save locally ends with a green "Saved to OneDrive" on screen. `storageBlockedReason` persists in memory but is read only by the startup block at 6656 — `renderSyncAlert:1089` and `listFreshness:1002` do not consider it — so the sticky channel the app uses for sync problems never carries this one. With 35 `setStatus` call sites sharing one line of screen, the transient channel is the only place a local-storage failure is ever stated. | Confidence: High

**P6-07** | Outputs | L | `index.html:2955` | The export/import route the app presents as a whole sync mechanism carries two of the three synced files; the trip archive has no path out. | `exportData:2955-2956` downloads `recipes-data.json` and `shopping-list.json` only, and its status line tells the user "Move both into your shared OneDrive folder" (2957). For a browser that cannot link a folder, 1160 presents this pair as the entire mechanism: "Use 'Export data' to save files into your shared drive folder manually, and 'Import data' on other devices to load them." `TRIPS_FILE` (`trip-history.json`, 2676) is written only by `pushTripHistory:2792` through the OneDrive or folder backends, and `importData:3009` has two branches — shopping and recipes — with no third. So a household on the manual route accumulates an archive that never reaches another device, and `cookRateSummary`/`atHomeStreaks` read only what this phone happened to finish. CLAUDE.md calls import "the recovery tool of last resort"; a restore through it returns the week and the recipes and silently drops the year of shops. | Confidence: High

**P6-08** | Outputs | L | `index.html:5321` | The printed list and the screen list apply different filters to the same data, and the printed copy silently reinstates lines the shopper has hidden. | The screen computes `visibleLines = shoppingData.shoppingList.filter(l=>!l.removed && !l.atHome)` (5027) and then `shownLines = hideTickedItems ? visibleLines.filter(l=>!l.checked) : visibleLines` (5033). `buildShoppingPrintSection:5320` applies only the first half — `.filter(l=>!l.removed && !l.atHome)` — and never consults `hideTickedItems`, which is a persisted toggle restored across sessions (`saveUiState:370`, `loadUiState:379`). So a shopper who has hidden ticked items and prints mid-shop gets them all back, with no note on the page saying the filter was dropped; every printed row is an unticked `☐` box (5339), including the items already in the trolley. The two paths agree on quantity rendering by explicit intent — `lineQtyText(line);   // same rendering as the on-screen list` (5336) — which is what makes the unstated disagreement about membership stand out. | Confidence: High
